import { CalcResizableTypes, MaxPositionByParentTypes } from '../types';
export declare const calcXPositionByMaxLeft: ({ prevLeft, prevWidth, minWidth, x }: {
    prevLeft: number;
    prevWidth: number;
    minWidth: number;
    x: number;
}) => number;
export declare const calcYPositionByMaxBottom: ({ maxBottom, y }: {
    maxBottom: number;
    y: number;
}) => number;
export declare const calcResizablePositionByClass: ({ className, pageX, pageY, prevX, prevY, prevTop, prevWidth, prevHeight, prevLeft, minWidth, maxBottom }: CalcResizableTypes) => any;
export declare const getMaxPositionByParent: ({ className, height, maxParentBottom, maxRight, width, x, y, maxWidth, maxHeight }: MaxPositionByParentTypes) => any;
