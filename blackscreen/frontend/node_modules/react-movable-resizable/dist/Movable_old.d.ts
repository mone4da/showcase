import * as React from 'react';
export declare type MovableProps = {
    useParentBounds: boolean;
    children: React.ReactNode;
};
declare const Movable: ({ useParentBounds, children }: MovableProps) => JSX.Element;
export default Movable;
